- For Select components, never coerces or converts data types — no String(), Number(), parseInt, or `as` casts. Select items must be an array of { label, value } objects where value keeps its native type, and onValueChange receives that typed value directly (no stringified round-trip). Confidence: 0.85
- Values clean code: prefers removing dead/unused code, inaccurate comments, and unnecessary complexity in a single pass. Confidence: 0.9
- Requests that plain JS files (e.g., static web workers outside the TS build) be made "type safe and clean" — expects full JSDoc/@ts-check type safety (ideally referencing shared types so client and worker can't drift) plus a cleanup pass (deduplicated types, extracted helpers, resource cleanup) in one request, rather than leaving loosely typed JS. Wants types imported once from the shared source of truth via `@typedef {import("...")}` (type-only, no runtime imports), but REJECTED deriving every signature via `Parameters<...>` / `ReturnType<...>` indexed access as too clever/ugly ("not like that, image worker ts like that maybe") — prefers the original TS-worker style: direct named types (`ImageFormat`, `EncodeImageOptions`, etc.) used plainly in JSDoc signatures, even if that means importing several named typedefs rather than exactly one. Readability of the JSDoc beats minimizing the type-import surface. Confidence: 0.8
- Values human-readable code and asks the agent to review its own newly created/modified files ("check whatever you created and what can be more human readable") — expects a readability pass that refactors verbose/repetitive JSX (e.g., repeated Tooltip/Button/icon blocks) into small, named local helper components rather than leaving long inline duplication, and prefers focused local extraction over introducing premature shared abstractions. Confidence: 0.7
- Dislikes type gymnastics that make code less readable — explicitly rejected `Parameters<...>`/`ReturnType<...>` indexed-access type derivation in JSDoc as "not like that," wanting the clean named-type style used in the original TS source instead. Plain, directly-readable type references are preferred over clever derived-type one-liners even when the latter reduce the number of imported types. Confidence: 0.7
- Enforces strict feature scope — when a task is about one feature (e.g., unlock-pdf), only that feature should be built; companion/inverse features (e.g., lock-pdf/encrypt) and their packages/test fixtures must be excluded from the change and removed from the project, not shipped alongside. Confidence: 0.7
- Prefers a simple light/dark theme toggle (a single button that flips between the two) over a dropdown with Light/Dark/System options — explicitly requested "no dropdown only light and dark toggle"; keeps theme UI minimal with no system/auto option. Confidence: 0.8
- Prevents code duplication — notices when separate modules share nearly identical scaffolding and questions whether they should be consolidated; user explicitly flags near-identical sibling service files (e.g., encrypt-pdf.ts and unlock-pdf.ts: "same file like encryption something") and expects the shared load/save/Blob scaffolding to be extracted into a common helper rather than left duplicated. Confidence: 0.8
- Prefers fixing bugs (especially logic bugs like missing `return` statements) at the same time as doing related refactoring work. Confidence: 0.8
- Prefers keeping shared service APIs simple and concrete (e.g., File-typed parameters) rather than generalizing inputs with union types (File | ArrayBuffer | Uint8Array | PDFDocument) — an over-engineered PdfSource union abstraction in the shared pdf utils was dropped in favor of simple File-only signatures. Confidence: 0.55
- Prefers centralized, worker-offloaded processing over main-thread implementations — moves resize into the shared Web Worker to avoid blocking the UI thread. Confidence: 0.8
- Prefers sibling features to share the same concrete UI component rather than bespoke equivalents — points at a reference file (e.g. "<feature>.tsx json viewer like src/features/json-formatter/components/json-formatter.tsx") and expects that exact shared editor/viewer to be adopted by the other feature (grammar per pane, line numbers, folding, highlighting), so tooling UX is identical across tools. Confidence: 0.65
- Prefers consistent UX across similar features (e.g., single-file download vs. auto-zip for multiple files should match the compressor pattern); when requesting a companion feature, expects it to mirror the sibling feature exactly — same folder layout, page layout (PageHeader/HowItWorks/FAQ), and UX patterns (encrypt-pdf built "similar to" unlock-pdf) rather than a bespoke implementation. Confidence: 0.8
- Wants type-null-safety properly handled (e.g., checking `null` return from `decodeAvif` instead of passing through unchecked). Confidence: 0.7
- Prefers removing or fixing inaccurate documentation/comments rather than leaving misleading ones — explicitly asks for docs (e.g., README) to be updated to match the actual project state (real tooling, commands, structure, tech stack) instead of stale content. Confidence: 0.85
- Values clear feature differentiation — features should have distinct, meaningful identities and real technical differences (e.g., actual compression techniques vs. just different quality defaults), not just cosmetic differences like naming or default settings. Confidence: 0.7
- Prefers using Bun as the project runtime/toolchain over npm or other Node.js runtimes. Confidence: 0.9
- Values keeping dependencies up to date — proactively requests updating libraries (e.g., @base-ui/react, shadcn) to their latest versions rather than staying on older releases. Confidence: 0.8
- Expects completed work to be committed and pushed to git (commit + push) rather than left uncommitted in the working tree — issues terse "commit and push" or bare "commit please" instructions after each task, with no further detail needed; a bare "commit" still implies the full flow (draft the commit message, commit, push the branch, and report the PR link). Confidence: 0.95
- Treats pasted PR review comment links (e.g., Greptile bot reviews) as actionable tasks: expects the review to be fetched, the flagged issues implemented across all affected files, and the result verified (build + rendered output) before reporting back. Confidence: 0.85
- Communicates in terse, abbreviated, lowercase instructions with typos (e.g., "Privacy page complete plese on footer", "about page also create", "create a new branch for implement unlock pdf", bare "commit"), expecting the agent to infer the full task, the codebase conventions, and the missing context without asking for clarification. Confidence: 0.97
- Delegates visual/design restructures with a high-level brief (e.g., "footer restructure and design please") and expects the agent to autonomously plan and execute the whole redesign — structure, layout, and visual treatment consistent with the site's existing design language (token-based styling, responsive grids) — and verify it with a build, without requiring a spec or back-and-forth. Confidence: 0.55
- Wants informational/legal pages (About, Privacy) to be complete, real pages linked from the site footer rather than placeholder links pointing to "/". Open Source is the exception: it does not belong in the footer — it should be relocated to its better-fitting home (homepage "Open Source Callout" + the footer brand column's GitHub icon) and the footer Project column kept lean with Home, About, Privacy only. Confidence: 0.7
- Cares about content placement: when an item doesn't look right in one location, prefers moving it to a better-fitting spot over deleting it or leaving a dead link — asks "any better placement?" rather than just removing the feature. Confidence: 0.6
- Wants upstream GitHub issues to be "proper," complete bug reports — build the project first, verify the facts (exact file sizes, tool/library versions, what references what in the output), and include repro steps, expected vs. actual behavior, root-cause analysis, and references in a copy-paste-ready body — rather than a vague one-liner. Confidence: 0.65
- When investigating a build/bug difference across deployment targets, wants both sides verified — explicitly asks to test the affected configuration and "the opposite also" (e.g., cloudflare vs nitro by commenting/uncommenting plugins in vite.config.ts), comparing each target's output artifacts (dist/ vs .output/) to confirm both reproduction and control. Confidence: 0.6
- When isolating a bug's root cause, wants confounding variables ruled out first: confirms the installed dependency is at the latest published version (so the bug isn't already fixed upstream) and verifies the control build shares the same toolchain instance (same Vite/Rolldown, same peer-dep resolution) as the buggy build — so the divergence can be attributed to the plugin's server-environment handling rather than the bundler. Confidence: 0.7
- When a general-purpose library has a confirmed bug in a critical path (e.g., `@cantoo/pdf-lib` 2.8.1 re-emitting stale encrypted xref streams on decrypt), prefers a purpose-built library for that exact use case (`@pdfsmaller/pdf-decrypt`) over a workaround in app code. Confidence: 0.6
- Wants flicker-free form UX: error messages shown inline directly below the relevant input (e.g., under the password field) and cleared automatically as soon as the user types, rather than appearing in a separate section/column that pops in and out. Also wants no false/transient errors from stale derived state — reported that pasting valid JSON into a JSON→YAML pane "split second showing invalid json" because the ~200ms debounce left the conversion describing the previously empty input; expects the error banner/border to be gated until the result reflects the current input (e.g., an `isPending = debouncedInput !== input` guard) so valid input never flashes an error. Confidence: 0.7
- Does NOT require dynamic/lazy imports for heavy third-party libraries (e.g., @cantoo/pdf-lib): explicitly stated that TanStack Router already emits code-split per-route chunks, so static imports are fine and already produce separate chunks in the build output; prefers plain static imports and updated project docs (AGENTS.md) and taste notes to reflect that lazy imports aren't strictly needed. Confidence: 0.85
- Verified when static imports don't defeat code splitting: checks the build output (e.g., pdf-lib emitted as its own separate chunk) before concluding lazy imports are required; when code splitting is already intact, updating project docs/taste notes (AGENTS.md) to reflect that lazy imports aren't strictly needed is acceptable. Confidence: 0.5
- Wants logic that is reusable across features (e.g., encryptPdf/unlockPdf services) centralized in a shared folder (src/shared/services) rather than living inside individual feature folders — explicitly requests "move to shared folder" so sibling features share one implementation. Confidence: 0.9
- Prefers consolidating all tightly-related functions for one domain (e.g., encryptPdf, unlockPdf, isPdfEncrypted) into a single cohesive file named after the concern (e.g., pdf-security.ts) instead of splitting them across one-file-per-function modules plus a separate utils file — explicitly suggested "all in one file name pdf-security maybe", and the agent's consolidation was accepted. Confidence: 0.7
- Prefers each feature change to be done on a dedicated new git branch — explicitly requests "create a new branch" as part of the task, instructing the full workflow "go to main branch, pull, and create a new branch" before any feature work starts. Confidence: 0.95
- When adding a new major feature (e.g., blog), prefers following the framework's own documented/recommended approach and official guides over third-party solutions or custom implementations — explicitly directs the agent to a specific TanStack Router guide URL and expects it to be implemented according to that guide's patterns. Confidence: 0.85
- Prefers jsdelivr's `https://cdn.jsdelivr.net/npm/<pkg>@<version>/+esm` URLs over esm.sh for ESM CDN imports in runtime code (e.g., web workers) — requested migrating "all esm sh" imports to the jsdelivr `+esm` format; expects the migration to be complete and consistent (no leftover esm.sh references, stale comments, or ambient type declarations pointing at the old CDN), including renaming the ambient type declaration file to match the new CDN. Confidence: 0.85
- Prefers naming files after the actual service/technology they reference rather than a stale or former one — explicitly asked to rename `types/esm-sh.d.ts` to `types/jsdelivr.d.ts` once the imports moved to jsdelivr, noting jsdelivr is itself a multi-CDN aggregator, even though the rename is cosmetic as far as tsc is concerned. Confidence: 0.55
- For password-protected features, wants a live password strength indicator (weak/medium/strong label + colored progress bar) shown on the form; the meter is purely informational — weak passwords must still be accepted (explicitly requested "add allow with weak pass"), not gated or blocked, and the indicator should never prevent submission. Confidence: 0.85
- Wants password forms to offer one-click generation that fills the fields, reusing the existing password-generator feature's utilities (generatePassword, calculateStrength, getPasswordStrength, DEFAULT_PASSWORD_OPTIONS) instead of reimplementing strength logic or generator code. Confidence: 0.8
- Reports broken features with terse bug reports (e.g., "Encrypt PDF not working why check", "after both password match still ecrypt pdf is disable") and expects the agent to autonomously diagnose the root cause — investigating the changed code, reproducing the failure, and fixing it — rather than asking clarifying questions or merely acknowledging the report. Confidence: 0.8
- Treats a form whose primary action button stays disabled even after validation passes (e.g., both password fields match) as a bug to diagnose and fix — expects the button to enable as soon as the inputs are valid. Confidence: 0.6
- Wants password fields on password-protected features to include copy-to-clipboard and show/hide (view) toggles alongside the existing generate option — explicitly requested "password copy and view option also add". Confidence: 0.7
- Wants destructive/redundant operations blocked, not just warned about: when the selected file is already in an incompatible state (e.g., an already password-protected PDF dropped into encrypt), the feature must show a visible warning, keep the action button disabled, and guard the handler so re-encrypting (which would overwrite the existing password) is impossible — with the message guiding the user to the correct tool (e.g., "Use the Unlock PDF tool first"). A warn-and-continue behavior was explicitly corrected to a hard block. Confidence: 0.6
- Prefers `js-yaml` (`load`/`dump`/`YAMLException`) over the `yaml` package for YAML parsing/serialization — pasted the js-yaml docs with "use this" to direct replacing the `yaml` dependency, and expects the swap to be complete (dependency removed, all usages/error handling updated, stale references like `optimizeDeps` cleaned up). Confidence: 0.5
- Prefers the `js-base64` package (`encode`/`decode`/`isValid`) over hand-rolled Base64 logic — pasted the import (`import { encode, decode } from 'js-base64'`) with "use this" to direct replacing a hand-rolled TextEncoder/btoa implementation; same pattern as directing library choices by pasting the import/docs and expecting the swap to be complete (helpers like the old toBase64Url removed, existing behavior/contracts preserved). Confidence: 0.55
- Prefers building new features on top of already-installed dependencies and existing shared services (zero new packages) rather than adding new libraries — explicitly frames ideation as "with existing libraris what other features we can build?" and expects proposals that reuse the current stack and mirror existing sibling features. Confidence: 0.5
- "Do not reinvent the wheel" — prefers established, well-maintained libraries over hand-rolled implementations: when a library covers the job (parsing, formatting, encoding, validation, etc.), use it and delete the custom code rather than keeping a hand-written version (e.g., js-base64 instead of TextEncoder/btoa Base64 logic, xml-formatter's formatXml/formatXml.minify with strictMode instead of a custom XML tokenizer/validator/formatter). When a library only lacks a minor detail (e.g., friendly error messages), prefers surfacing the library's own error over rebuilding the whole implementation. Confidence: 0.9
- Prefers compact form layouts on desktop: controls should be arranged two-per-row on wider screens (e.g., `grid grid-cols-1 sm:grid-cols-2` pairing Size + Quiet Zone, Error Correction + Background Color) rather than stacked full-width, to reduce the module's vertical height and scrolling — explicitly requested "some controls can be two in a row in desktop, make it compact somehow". Related controls get paired together while full-width elements (like color swatch rows) can span the whole row. Confidence: 0.8
- Prefers folder-based route file organization following the framework's documented convention (actual directories with `index.tsx`, `$param.tsx`, layout files) over flat dot-notation naming (e.g., `image-converter.index.tsx` in the routes root) — explicitly shared the TanStack Router docs folder structure and asked for routes to be organized in real folders matching that convention. Confidence: 0.9
- Prefers utility features (e.g., QR generator) to support the standard set of content types — URL, text, email, phone, SMS, geo/location, WiFi, and vCard/contact — with a live preview panel, not just the basic types. Confidence: 0.55
- Wants output-format choices for generated assets (e.g., PNG download + SVG download + copy-to-clipboard for QR codes) and sensible presets (size, color swatches, error-correction, quiet zone) instead of a single fixed output. Confidence: 0.5
- Wants user-facing validation that blocks bad output before it's produced (e.g., a WCAG-style contrast check that errors on low-contrast QR colors, auto-prefixing `https://` for bare URLs, escaping special characters in WiFi payloads) with clear inline error messages. Confidence: 0.55
- Prefers simple error reporting that surfaces the underlying parser's own message (`JSON.parse`, js-yaml's `YAMLException`, the browser `DOMParser`) rather than building custom line/column position-mapping or message-scraping machinery. When shown a position scanner, explicitly directed dropping the location machinery entirely ("simplify to just the parser message, no line/column") instead of propping it up, and expects the simplification to extend to sibling features with the same pattern. Confidence: 0.75
- Prefers tuple-return error handling over inline try/catch: a shared `safe`/`safeSync` helper returning `[value, null] | [null, error]` (taking a function for the sync case so the work runs inside the try), applied consistently across every affected call site — both services and components — and exported from the shared utils barrel. Confidence: 0.7
- Prefers consolidating clustered/coupled component state into a single `useImmer` store instead of multiple separate `useState` hooks — explicitly requests "use useimmer instead of multiple state" when a component has several interrelated state values, so multi-field mutations become one atomic draft update (e.g., base64 `{ input, mode, alphabet }`, xml-formatter `{ input, indent, operation }`, with the state type added to the feature's `types/index.ts`). Confidence: 0.8
- When refactoring to `useImmer`, keeps small named setter handlers (`setInput`, `setMode`, `setOperation`) rather than inlining `updateState((draft) => ...)` callbacks at every JSX call site — inline draft updaters make the JSX noticeably more verbose for what are one-line handlers; the JSX itself is left otherwise unchanged. Confidence: 0.6
- Approves the agent's proposed plan with a single terse word (e.g., a bare "apply") and expects it to be executed exactly as described — no re-confirmation, no scope changes, no questions. Confidence: 0.6
- For image/visual tools, prefers rich, interactive output over a static list of results: wants derived data visualized back onto the source (e.g., markers showing where each extracted color actually sits in the image) and directly manipulable afterwards (e.g., draggable markers that re-sample the color under them), with hover linking between the controls and the visual (hover a swatch → spotlight its marker on the image). Confidence: 0.6
- Wants front-end changes verified in a real browser against the running dev server, not just typecheck/tests/build — proactively installs and directs the agent to browser tooling (agent-browser) and points it at the local URL (e.g., "I have installed agent browser check http://localhost:3000/<tool>"), expecting the agent to open the page, upload/exercise the real UI, and confirm the interactions and visuals actually work. Confidence: 0.8
